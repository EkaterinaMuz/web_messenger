<dl class="{{styles.row}}">
    <dt class="{{styles.label}}">{{ fieldName }}</dt>
    <dd class="{{styles.value}}">{{ fieldValue }}</dd>
</dl>
{{#unless @last}}
    <hr class="{{styles.separator}}" />
{{/unless}}
